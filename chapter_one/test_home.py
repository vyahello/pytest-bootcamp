"""
Write tests for `square()` function function which will cover positive and negative scenarios.

	- At least one test has to be parametrized
	- At least one test has to use  `with pytest.raises(...)` statement
	- All tests have to be marked with `positive` or `negative` markers.
"""

from chapter_one.operation import square
